for(int i = 0; i < tamanhoVetor.length; i++){
    if([i] == null){
        vetor[posicaoEncontrada]--;
    }
    if(vetor[0] != null && vetor.proximo() != null){
        System.out.println(vetor[i])
    }
}




// Implemente na classe FilaVetor o método abaixo:

// public void encolher()

// Este método deverá realocar o vetor info de forma que após esta operação, o vetor utilize apenas o espaço mínimo necessário para armazenar os dados enfileirados. Por exemplo, suponha que uma fila contenha o vetor abaixo, e que o atributo inicio seja igual à 3 e o atributo tamanho igual à 4:

// Após a execução do método encolher(), o vetor info deverá possuir o seguinte conteúdo:

// Não esqueça que a fila pode utilizar o vetor de forma circular.

// Para publicar a solução, copie e cole o método encolher() que você implementou.

// Serão avaliadas lógica e eficiência da implementação. 